#include <stdlib.h>
#include <time h>

/**
 *  Determines if a number is positive, negative or zero
 *
 *  Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand0 - RAND_MAX / 2;
	if (n > 0)
	{
		printf("%d is positivein", n),
	}
	else if (n == 0)
}
else
{
	printf("%d is negativeln", n),
}
return (0),
